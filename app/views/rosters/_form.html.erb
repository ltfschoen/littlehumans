<%= form_for(@roster) do |f| %>
  <% if @roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roster.errors.count, "error") %> prohibited this roster from being saved:</h2>

      <ul>
      <% @roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_shift %><br>
    <%= f.text_field :name_shift %>
  </div>
  <div class="field">
    <%= f.label :name_hospital %><br>
    <%= f.text_field :name_hospital %>
  </div>
  <div class="field">
    <%= f.label :name_location %><br>
    <%= f.text_field :name_location %>
  </div>
  <div class="field">
    <%= f.label :time_shift_start %><br>
    <%= f.datetime_select :time_shift_start %>
  </div>
  <div class="field">
    <%= f.label :time_shift_end %><br>
    <%= f.datetime_select :time_shift_end %>
  </div>
  <div class="field">
    <%= f.label :time_shift_duration %><br>
    <%= f.text_field :time_shift_duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
